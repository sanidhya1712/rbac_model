import { filters } from "../types/permission";
export const getPolicies = (token: string, filters: filters = null) => {

}

export const updatePolicies = (token: string, filters: filters = null) =>{

}