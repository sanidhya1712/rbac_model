{
  "name": "rbac_model",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=local PORT=5001 npx ts-node-dev --transpile-only app.ts",
    "build": "npx rimraf lib && npx tsc"
  },
  "files": ["lib/**/*"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1346.0",
    "axios": "^1.3.4",
    "typescript": "^5.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.15.11"
  }
}
